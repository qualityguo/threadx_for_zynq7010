## 第一章：操作系统移植（基于版本6.4.1）

### 一、项目工程结构

- `BSP`：外设驱动代码
- `ThreadX/Source`：存储操作系统源码
- `ThreadX/Port`：操作系统的移植层
- `ThreadX/utility`：相关组件
- `main.c`：任务管理文件
- `includes.h`：头文件汇总
- `ThreadX`
  - `tx_port.h`：移植头文件
  - `tx_user.h`：ThreadX配置文件


### 二、代码拷贝

- `/threadx/common/inc/`到`ThreadX/Source`目录下
- `/threadx/common/src/`到`ThreadX/Source`目录下
- `/threadx/ports/cortex_a9/gnu/src/*`到`ThreadX/Port`目录下（拷贝文件）
- `/threadx/ports/cortex_a9/gnu/inc/tx_port.h`到根目录下的`ThreadX/tx_port.h`
- `/threadx/ports/cortex_a9/gnu/example_build/*.h&*.S`到`ThreadX/Port`目录下（拷贝文件）
- `/threadx/utlity/execution_profile_kit/*.s&*.c`到`ThreadX/utlity/execution_profile_kit/`目录下（拷贝文件）
- 删除`ThreadX/Port`目录下的FIQ相关汇编
  - `tx_thread_fiq_context_save.S`（FIQ上下文保存）
  - `tx_thread_fiq_context_restore.S`（FIQ上下文恢复）
  - `tx_thread_fiq_nesting_start.S`（FIQ嵌套开始）
  - `tx_thread_fiq_nesting_end.S`（FIQ嵌套结束）

- 删除`ThreadX/Port`目录下的IRQ嵌套相关汇编
  - `tx_thread_irq_nesting_start.S`（IRQ嵌套开始）
  - `tx_thread_irq_nesting_end.S`（IRQ嵌套结束）


### 三、代码修改

- `lscript.ld`中修改

  - ```
    + _sp = __undef_stack;
    + _stack_bottom = _stack_end;
    
    _end = .;
    ```

  - 将应用控制权交给ThreadX之后，重设栈指针

- `tx_user.h`中参考`/threadx/common/inc/tx_user_sample.h`

  - ~~加入宏~~`#define TX_ENABLE_FIQ_SUPPORT`（不适用FIQ，不必加入）
  - 加入宏`#define TX_EXECUTION_PROFILE_ENABLE`

- 修改`ThreadX/Port/v7.s`名称为`ThreadX/Port/v7.S`

- 删除`ThreadX/Port/crt0.S`

  - 修改`tx_thread_schedule.s`中的`__tx_thread_schedule_loop`实现

    - ```c
      # 原有的
      __tx_thread_schedule_loop:
      
          LDR     r0, [r1]                        // Pickup next thread to execute
          CMP     r0, #0                          // Is it NULL?
          BEQ     __tx_thread_schedule_loop       // If so, keep looking for a thread
              
      # 现在的
      __tx_thread_schedule_loop:
      
          LDR     r0, [r1]                        // Pickup next thread to execute
          CMP     r0, #0                          // Is it NULL?
          BNE     __tx_thread_have_thread         // If not NULL, we have a thread
              
          DSB
          WFI
          ISB
          B		__tx_thread_schedule_loop		// Loop back to check for thread
              
      __tx_thread_have_thread:
      ```

    - 这样在没有新的任务的时候会进入`WFI`，然后等待tick唤醒

    - **每次Tick都会唤醒**

- 修改`reset.S`中的文件向量表的位置

  - ```c
    // 将__vectors中断向量表放在vectors段中，xilinx的BSP对应的_vector_table后边
    .section .vectors, "ax"
    .align 5
    .global __vectors
    ```

  - 否则会应为向量表地址不对齐造成异常中断

- 修改`tx_initialize_low_level.S`

  - ```c
    # _tx_initialize_low_level函数入口保存lr
    MOV		r12, lr
    # PUSH lr的时候替换为PUSH r12    
    PUSH	{r12}								// Push saved return address(in r12)
    ```

  - tick时间不准

    - CPU主频666.666MHz

    - 私有定时器主频333.333MHz

    - 原先重载值0xF0000，等于983940，一个tick的时间是983940/33333333=2.95ms，默认的Tick是10ms，快了3.4倍，应修复为**3333333**

    - ```c
      - MOV r0, #0xF0000
      + LDR r0, =3333333
      ```

- 修改`tx_initialize_low_level.S`

  - ```c
    // 修改中断屏蔽等级-与BSP一致
    // 0x1F设置为0xF0
    //
    // Enable Private Timer for periodic IRQ
    // --------------------------------------
    MOV     r0, #0xF0
    BL      setPriorityMask               // Set priority mask (local)
    ```

  - ```c
    // 加入中断分发函数的声明
    .global		tx_irq_dispatch
    
    // timer处理完之后直接到after_irq_dispatch
        BL     _tx_timer_interrupt                  // Timer interrupt handler
        B	   after_irq_dispatch					// skip tx_irq_dispatch for timer
    // 加入分发函数的调用(加入前两行)
    by_pass_timer_interrupt:
        MOV     r0, r4
        BL		tx_irq_dispatch						// dispatch
            
    after_irq_dispatch:
        MOV		r0, r4
        BL      writeEOI
        POP     {r4, r5}                            // Recover preserved registers
    
        /* Jump to context restore to restore system context.  */
        B       _tx_thread_context_restore
    ```

- bsp文件夹中加入函数`bsp_init`，在`includes.h`包含，在`main`函数中调用

  - 其中完成GTC的初始化（我们采用32bit的计数器做性能评估）

  - GIC的初始化（为了中断调用Xilinx的API）、中断分发函数

  - ```c
    #define GTIMER_COUNTER_LO   (*(volatile u32 *)0xF8F00200)
    #define GTIMER_COUNTER_HI   (*(volatile u32 *)0xF8F00204)
    #define GTIMER_CONTROL      (*(volatile u32 *)0xF8F00208)
    
    /* 只使用1个32bit的值用作性能测试 */
    static void Global_Timer_Init()
    {
    	GTIMER_CONTROL    = 0x00;
    	GTIMER_COUNTER_LO = 0;
    	GTIMER_COUNTER_HI = 0;
    	GTIMER_CONTROL    = 0x01;
    }
    
    /* GIC初始化 */
    static void GIC_Init()
    {
    	XScuGic_Config *intc_cfg;
    	intc_cfg = XScuGic_LookupConfig(INTC_DEVICE_ID);
    	XScuGic_CfgInitialize(&xInterruptController, intc_cfg, intc_cfg->CpuBaseAddress);
    }
    
    /*
     * 中断分发函数(汇编by_pass_timer_interrupt调用)
     */
    void tx_irq_dispatch(unsigned int int_id)
    {
    	extern XScuGic xInterruptController;
    
    	if(int_id < XSCUGIC_MAX_NUM_INTR_INPUTS)
    	{
    		XScuGic_VectorTableEntry * entry =
    			&xInterruptController.Config->HandlerTable[int_id];
    		entry->Handler(entry->CallBackRef);
    	}
    }
    
    void bsp_init()
    {
    	Global_Timer_Init();
        GIC_Init();
    }
    ```

- `tx_execution_profile.h`中修改读取时间的寄存器

  - ```c
    #ifndef TX_EXECUTION_TIME_SOURCE
    #define TX_EXECUTION_TIME_SOURCE         (EXECUTION_TIME_SOURCE_TYPE) *((volatile ULONG *) 0xF8F00200)
    #endif
    #ifndef TX_EXECUTION_MAX_TIME_SOURCE
    #define TX_EXECUTION_MAX_TIME_SOURCE     0xFFFFFFFF
    #endif

- `includes.h`加入头文件，`#include "bsp_init.h"`

  - ```c
    extern EXECUTION_TIME     _tx_execution_thread_time_total;
    extern EXECUTION_TIME     _tx_execution_isr_time_total;
    extern EXECUTION_TIME     _tx_execution_idle_time_total;
    ```


### 四、SDK配置

- 加入头文件搜索路径：
  - `ThreadX/Source/inc`
  - `ThreadX/Port/`
  - `./`
  - `BSP/`
  - `ThreadX/utility/execution_profile_kit`
- 加入宏`TX_INCLUDE_USER_DEFINE_FILE`，加入`tx_user.h`

## 第二章：系统启动相关分析

### 一、Xilinx的启动流程

- 关键的几个文件
  - `asm_vectors.S`
  - `boot.S`
  - `cpu_init.S`
  - `translation_table.S`
  - `xil-crt0.S`
- 初始化流程（以单核CPU0为例）
  - 上电后执行`_boot`函数，位于异常向量表的首地址
  - 执行`CheckEFUSE`函数，做了一些检查后跳转`OKToRun`
  - `OKToRun`
    - 处理两个BUG
    - 设置VBAR
    - 无效化缓存和TLB
    - 禁用MMU
    - 设置处理器各种模式的栈指针
    - 使能SCU
    - 使能MMU
    - 配置ACTLR
    - 初始化并使能L2Cache
    - 使能p10和p11的全访问
    - 使能FPU
    - 使能分支预测和预取址
    - 使能异步中断异常
    - 跳转`start`
  - `start`函数
    - 执行`__cpu_init`，清空cp15中复位值未知的寄存器，复位并启动周期计数器
    - 清SBSS段
    - 清BSS段
    - 设置System模式的栈指针
    - 初始化一个全局定时器
    - 跳转`main`

### 二、ThreadX移植层文件的启动流程

- 关键的几个文件
  - `reset.S`
  - `crt0.S`
  - `tx_initialize_low_level.S`
- 初始化流程
  - 异常向量在异常向量表中的顺序和裸机环境下是一致的
  - 上电后会执行`_start`
  - `_start`函数
    - 初始化SP
    - 设置栈限制寄存器SL
    - 清零栈指针
    - 清零BSS段
    - 跳转`main`
  - `main`函数
    - 调用`tx_kernel_enter`
    - 调用`tx_initialize_kernel_enter`
    - 调用`_tx_initialize_low_level`
    - 调用一些初始化后进入一个调度器
  - `_tx_initialize_low_level`函数
    - 设置处理器各种模式的栈指针，SYS模式、FIQ模式、IRQ模式
    - 计算可用内存
    - 设置中断向量表
    - 初始化GIC
    - 设置中断优先级
    - 配置私有定时器中断
    - 使能SGI
- 不同对比
  - 栈空间设置（复用栈空间）
    - 裸机环境下的栈空间
      - SYS模式栈：8KB
      - IRQ模式栈：1KB
      - SVC模式栈：2KB
      - Abort模式栈：1KB
      - FIQ模式栈：1KB
      - UBDEF模式栈：1KB
    - ThreadX环境下的栈空间
      - SVC栈：12.5KB
      - IRQ栈：1KB
      - FIQ栈：0.5KB

## 第三章：参考设计

- 任务栈

  - 栈的空间采用静态分配

  - TCB中存储栈的一些信息，可以得到最大栈使用

    - ```c
      tx_thread_stack_end				// 栈底
      tx_thread_stack_ptr				// 实时栈顶
      tx_thread_stack_highest_ptr		// 最大栈顶
      ```

  - 栈检测使能

    - ```c
      // 使能栈填充
      #define TX_ENABLE_STACK_CHECKING
      // 注册栈出错回调函数
      tx_thread_stack_error_notify(my_stack_error_handler);
      // 回调函数中-可以停止并打印栈信息
      ```

- 任务优先级

  - 值越大，优先级越低，默认是0-31
  - 分配（优先级由高到低）
    - IRQ任务（阻塞实现）：中断服务函数中触发的任务
    - 高优先级后台任务（阻塞实现）：比如串口消息处理
    - 低优先级时间片调度任务：比如GUI显示
    - 空闲任务（设置了WFI，不需要）

- 空闲任务

  - 空闲任务中可以休眠、停机等降低CPU使用率
  - 但是这个项目中，我们在调度器的汇编代码中加入了`WFI`指令，不需要手动空闲任务了

- 任务调度

  - 抢占式调度：高优先级的就绪任务
  - 时间片调度：同优先级，时间片用完则调度
  - 轮询调度：很少使用

- 临界区

  - 中断锁：开关中断实现
  - 任务锁：ThreadX有抢占阈值，uCOS给调度器加锁，RTX关闭内核定时器
  - 调度锁：禁止调度，但不是不禁止中断
  - ThreadX的抢占阈值
    - 例如任务优先级是20，设置抢占阈值是15，则只有优先级为0-14的任务可以抢占

- 消息队列

  - ThreadX、RTX5、FreeRTOS消息队列传递了数据
  - RTX4、uCOSII和uCOSIII消息队列传递了地址

- 内存块和内存池

  - 内存块计算：每个内存块多占据一个指针的大小

## 第四章：API介绍

- 任务管理

  - | 函数                      | 说明                                                         | 可以执行                     | 不可以执行 |
    | ------------------------- | ------------------------------------------------------------ | ---------------------------- | ---------- |
    | tx_kernel_enter           | 启动ThreadX调度器                                            | main函数                     | -          |
    | tx_thread_create          | 创建任务，使能抢占阈值将禁用时间片，尽量相同优先级任务才分配时间片 | 初始化、任务                 | 中断       |
    | tx_thread_delete          | 删除任务，不允许自己删除自己，只能删除终止态或完整态         | 任务、定时器组               | 中断       |
    | tx_thread_suspend         | 挂起任务                                                     | 初始化、任务、定时器组、中断 |            |
    | tx_thread_resume          | 恢复任务                                                     | 初始化、任务、定时器组、中断 |            |
    | tx_thread_reset           | 复位任务，只有复位终止态或完整态                             | 任务                         | 中断       |
    | tx_thread_terminate       | 终止任务，可以自己终止自己，可以终止指定任务                 | 任务、定时器组               | 中断       |
    | tx_thread_priority_change | 任务优先级修改，任务的抢占阈值自动设置为新优先级             | 任务、定时器组               |            |
    | tx_thread_info_get        | 获得任务信息                                                 | 初始化、任务、定时器组、中断 |            |

- 时间

  - | 函数            | 说明                     | 可以执行             | 不可以执行 |
    | --------------- | ------------------------ | -------------------- | ---------- |
    | tx_thread_sleep | 任务延迟，单位是时钟节拍 | 任务                 | 中断       |
    | tx_time_get     | 获得当前的时钟节拍       | 任务、定时器组、中断 |            |

- 事件标志组

  - | 函数                      | 说明                   | 可以执行                     | 不可以执行 |
    | ------------------------- | ---------------------- | ---------------------------- | ---------- |
    | tx_event_flags_create     | 创建事件标志组         | 初始化、任务                 |            |
    | tx_event_flags_set        | 设置事件标志           | 初始化、任务、定时器组、中断 |            |
    | tx_event_flags_get        | 设置事件标志           | 初始化、任务、定时器组、中断 |            |
    | tx_event_flags_set_notify | 事件标志组消息通知函数 | 关注的事件标志被设置时调用   |            |

- 消息队列

  - | 函数             | 说明         | 可以执行                     | 不可以执行             |
    | ---------------- | ------------ | ---------------------------- | ---------------------- |
    | tx_queue_create  | 创建消息队列 | 初始化、任务                 |                        |
    | tx_queue_send    | 发送消息     | 初始化、任务、定时器组、中断 | 中断必须使用TX_NO_WAIT |
    | tx_queue_receive | 接收消息     | 初始化、任务、定时器组、中断 | 中断必须使用TX_NO_WAIT |

- 信号量

  - | 函数                | 说明       | 可以执行                     | 不可以执行                               |
    | ------------------- | ---------- | ---------------------------- | ---------------------------------------- |
    | tx_semaphore_create | 创建信号量 | 初始化、任务                 |                                          |
    | tx_semaphore_put    | 发送信号量 | 初始化、任务、定时器组、中断 |                                          |
    | tx_semaphore_get    | 接收新哈量 | 初始化、任务、定时器组、中断 | 初始化、定时器组、中断必须使用TX_NO_WAIT |

- 互斥量（优先级天花板）

  - | 函数            | 说明       | 可以执行                     | 不可以执行             |
    | --------------- | ---------- | ---------------------------- | ---------------------- |
    | tx_mutex_create | 创建互斥量 | 初始化、任务                 |                        |
    | tx_mutex_put    | 释放互斥量 | 初始化、任务、定时器组、中断 |                        |
    | tx_mutex_get    | 获取互斥量 | 初始化、任务、定时器组、中断 | 中断必须使用TX_NO_WAIT |

- 软件定时器

  - | 函数              | 说明         | 可以执行               | 不可以执行 |
    | ----------------- | ------------ | ---------------------- | ---------- |
    | tx_timer_create   | 创建定时器组 | 初始化、任务           |            |
    | tx_timer_activate | 激活定时器   | 初始化、任务、定时器组 |            |

- 动态内存管理

  - | 函数                  | 说明           | 可以执行                     | 不可以执行 |
    | --------------------- | -------------- | ---------------------------- | ---------- |
    | tx_block_pool_create  | 内存块创建     | 初始化、任务                 |            |
    | tx_block_allocate     | 申请内存块     | 初始化、任务、定时器组、中断 |            |
    | tx_block_release      | 释放内存块     | 初始化、任务、定时器组、中断 |            |
    | tx_byte_pool_create   | 内存池创建     | 初始化、任务                 |            |
    | tx_byte_allocate      | 申请内存池     | 初始化、任务（申请时间不定） |            |
    | tx_byte_release       | 释放内存池     | 初始化、任务                 |            |
    | tx_byte_pool_info_get | 获得内存池信息 | 初始化、任务、定时器组、中断 |            |

## 第五章：使用TraceX

### 一、代码修改

- `tx_user.h`中使能Trace

  - ```c
    /* 使能事件Trace，会稍微增加点代码 */
    #define TX_ENABLE_EVENT_TRACE
    #define TX_TRACE_TIME_SOURCE                    *((ULONG *) 0xF8F00200)
    #define TX_TRACE_TIME_MASK                      0xFFFFFFFFUL

- `main.c`中加入缓冲区，并启动跟踪

  - ```c
    #define TRACE_BUFFER_SIZE 			(500 * 32)  		/* 500个事件的存储空间 */
    #define TRACE_MAX_OBJECTS  			20         			/* 最多跟踪的ThreadX对象数量 */
    UCHAR 	g_trace_buffer[TRACE_BUFFER_SIZE];				/* 缓冲区对象 */
    ```

  - ```c
    // AppTaskStart任务中
    /* 启动任务跟踪 */
    tx_trace_enable(&g_trace_buffer, 					/* 存储地址 */
                    TRACE_BUFFER_SIZE,					/* buffer大小 */
                    TRACE_MAX_OBJECTS);					/* 对象数量 */
    ```

### 二、vitis插件导出数据

- 双击`jtag_tool/run_xsct.bat`，并选择选项3即可，之后使用TraceX的软件打开

## 第六章：文件系统移植（基于版本6.4.1）

### 一、项目工程结构

- `FileX/Source`存储源码
- `FileX/Port`存储移植层文件

### 二、代码拷贝

- `/filex/common/`的文件拷贝到`FileX/Source`目录下
- `/filex/ports/generic/inc/fx_port.h`拷贝到`FileX/Port`目录下

### 三、代码修改

- 加入文件`fx_zynq_sdio_driver.h`和`fx_zynq_sdio_driver.c`
  - 实现`fx_zynq_sdio_driver`函数，等待在`fx_media_open`中调用

### 四、SDK配置

- 加入头文件搜索路径：
  - `FileX/`
  - `FileX/inc/`

## 第七章：文件系统参考设计

- 移植层

  - 实现`VOID fx_zynq_sd_driver(FX_MEDIA *media_ptr);`函数内部处理不同的请求。

  - ```c
    #define FX_DRIVER_READ                         0				// 读扇区
    #define FX_DRIVER_WRITE                        1				// 写扇区
    #define FX_DRIVER_FLUSH                        2				// 刷新缓存(没实现)
    #define FX_DRIVER_ABORT                        3				// 终止(没实现)
    #define FX_DRIVER_INIT                         4				// 初始化操作(在系统启动时初始化-此处空操作)
    #define FX_DRIVER_BOOT_READ                    5				// 读引导扇区
    #define FX_DRIVER_RELEASE_SECTORS              6				// 释放扇区(没实现-磨损均衡可能使用)	
    #define FX_DRIVER_BOOT_WRITE                   7				// 写引导扇区
    #define FX_DRIVER_UNINIT                       8				// 反初始化(没实现)
    ```

- 应用层

  - ```c
    uint32_t media_memory[64*1024];
    FX_MEDIA     sdio_disk;
    fx_system_initialize();			// 使用FileX必须初始化
    fx_media_open(&sdio_disk, "ZYNQ_SDIO_DISK", fx_zynq_sd_driver, 0, media_memory, sizeof(media_memory));			// 挂载SD卡
    //...
    fx_media_close(&sdio_disk);		// 卸载SD卡  
    ```

## 第八章：文件系统API介绍

- 介质操作

  - | API                               | 介绍                 | 参数                                     |
    | --------------------------------- | -------------------- | ---------------------------------------- |
    | fx_media_open                     | 打开存储介质         | 需要传入移植层的驱动函数、介质内存缓冲区 |
    | fx_media_close                    | 卸载存储介质         |                                          |
    | fx_media_flush                    | 缓存数据刷新到Cache  |                                          |
    | fx_media_extended_space_available | 查询存储介质可用空间 |                                          |

- 文件操作

  - | API            | 介绍         | 参数                |
    | -------------- | ------------ | ------------------- |
    | fx_file_create | 创建新文件   | 传入介质指针+文件名 |
    | fx_file_open   | 打开文件     | 写模式或读模式      |
    | fx_file_close  | 关闭文件     |                     |
    | fx_file_read   | 读取文件内容 |                     |
    | fx_file_write  | 写入文件内容 |                     |
    | fx_file_seek   | 读写指针偏移 |                     |
    | fx_file_delete | 删除文件     | 传入介质指针+文件名 |

- 目录操作

  - | API                               | 介绍       | 参数                           |
    | --------------------------------- | ---------- | ------------------------------ |
    | fx_directory_create               | 创建目录   | 多级目录需要完整的路径         |
    | fx_directory_delete               | 删除目录   | 目录必须为空才能删除           |
    | fx_directory_next_full_entry_find | 遍历目录项 | 返回文件名、属性、大小、事件戳 |

- 所有操作都是在打开介质和关闭介质之间操作


## 第七章：网络协议栈移植（基本版本6.4.1）

### 一、项目工程结构

- `NetXDuo/Port`目录，存储移植层文件

### 二、参考设计

- 移植层的port文件中，需要实现函数`VOID nx_driver_zynq(NX_IP_DRIVER *driver_req_ptr);`，处理不同的请求

- ```c
  #define NX_LINK_PACKET_SEND                        0		// 发送一个普通的网络包
  #define NX_LINK_INITIALIZE                         1		// 初始化硬件
  #define NX_LINK_ENABLE                             2		// 使能接口
  #define NX_LINK_DISABLE                            3		// 禁止接口
  #define NX_LINK_PACKET_BROADCAST                   4		// 发送广播包
  #define NX_LINK_ARP_SEND                           5		// 发送ARP请求包
  #define NX_LINK_ARP_RESPONSE_SEND                  6		// 发送ARP应答包
  #define NX_LINK_RARP_SEND                          7		// 发送RARP包
  #define NX_LINK_MULTICAST_JOIN                     8		// 加入多播组
  #define NX_LINK_MULTICAST_LEAVE                    9		// 离开多播组
  #define NX_LINK_GET_STATUS                         10		// 获取链路状态
  #define NX_LINK_GET_SPEED                          11		// 获得当前网速
  #define NX_LINK_GET_DUPLEX_TYPE                    12		// 获得双工类型
  #define NX_LINK_GET_ERROR_COUNT                    13		// 获得错误帧计数
  #define NX_LINK_GET_RX_COUNT                       14		// 获得接收成功包计数
  #define NX_LINK_GET_TX_COUNT                       15		// 获得发送成功包计数
  #define NX_LINK_GET_ALLOC_ERRORS                   16		// 获得分配失败计数
  #define NX_LINK_UNINITIALIZE                       17		// 反初始化
  #define NX_LINK_DEFERRED_PROCESSING                18		// 延迟处理触发
  #define NX_LINK_INTERFACE_ATTACH                   19		// 动态添加另一个接口
  #define NX_LINK_SET_PHYSICAL_ADDRESS               20		// 设置MAC地址
  #define NX_INTERFACE_CAPABILITY_GET                21		// 获得硬件能力
  #define NX_INTERFACE_CAPABILITY_SET                22		// 设置硬件能力
  #define NX_LINK_INTERFACE_DETACH                   23		// 分离已经添加的接口
  #define NX_LINK_FACTORY_ADDRESS_GET                24		// 获得出厂预设的MAC地址
  #define NX_LINK_RX_ENABLE                          25		// 单独启动接收
  #define NX_LINK_RX_DISABLE                         26		// 单独禁用接收
  #define NX_LINK_6LOWPAN_COMMAND                    27 		// 设置6LoWPAN
  #define NX_LINK_GET_INTERFACE_TYPE                 28		// 获得接口类型
  #define NX_LINK_RAW_PACKET_SEND                    29		// 发送原始以太网帧
  
  #define NX_LINK_USER_COMMAND                       50 /* Values after this value are reserved for application.  */
  ```

- 根据STM32H7系列的参考，需要实现的接口包括

  - ```c
    #define NX_LINK_INTERFACE_ATTACH                   19		// 动态添加另一个接口
    #define NX_LINK_INITIALIZE                         1		// 初始化硬件
    #define NX_LINK_ENABLE                             2		// 使能接口
    #define NX_LINK_DISABLE                            3		// 禁止接口
    #define NX_LINK_ARP_SEND                           5		// 发送ARP请求包
    #define NX_LINK_ARP_RESPONSE_SEND                  6		// 发送ARP应答包
    #define NX_LINK_PACKET_BROADCAST                   4		// 发送广播包
    #define NX_LINK_RARP_SEND                          7		// 发送RARP包
    #define NX_LINK_PACKET_SEND                        0		// 发送一个普通的网络包
    #define NX_LINK_MULTICAST_JOIN                     8		// 加入多播组
    #define NX_LINK_MULTICAST_LEAVE                    9		// 离开多播组
    #define NX_LINK_GET_STATUS                         10		// 获取链路状态
    #define NX_LINK_DEFERRED_PROCESSING                18		// 延迟处理触发
    ```

  - 添加接口

    - 对于单网口方案，直接返回SUCCESS

  - 初始化网口

    - 














